# 7th-Repo
This assignment asks the user to enter a series of numbers to start. It will then ask the user to enter a value. It uses the while function to display the value for 0 to stop the program from running. It then uses the if statament to print the numbers and display the range using max(numbers) - min(numbers). It then will print the range value. 
